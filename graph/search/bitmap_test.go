package gsearch

import "testing"

func TestSetBits(t *testing.T) {
	// bm := &Bitmap2D{dimX: 8, dimY: 8}
	// bm.bits = make([]byte, bm.dimX*bm.dimY/8)

	// msize := y*bm.dimX + x
	// a := msize / 8
	// b := msize % 8
	// bm.bits[a] |= (1 << b)
	t.Error(1 << 0)
}
